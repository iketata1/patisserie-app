import { environment } from 'src/environments/environment';

export const API_URL  = environment.API_BASE;
export const FILES_BASE = environment.FILES_BASE;
export const AI_BASE = environment.AI_BASE;
